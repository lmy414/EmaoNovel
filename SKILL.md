---
name: novel-writing-workflow
description: 小说创作助手，写小说、码字、网文写作、轻小说创作都用得上。帮你整理角色设定和世界观、初始化小说项目、批量修订小说内容（把XX改成YY）、继续上次写作、归档素材、管理剧情大纲。也适用于需要Git版本管理的创作场景。典型触发：整理这些设定、初始化小说项目、把门派改成学院、继续上次写、今天先到这、帮我归档世界观、写角色设定、修订大纲。
---

# 基于 Git 的小说创作工作流

## 概述

以 Git 为版本管理核心、AI 助手为执行层的小说创作工作流。作者负责"写什么"，AI 负责"怎么存"。

## 任务目标

- 本 Skill 用于：小说创作项目的高效管理与迭代
- 能力包含：原始素材归档、四类设定文件生成、Git 版本管理、批量修订、断点续写
- 触发条件：开始新项目、口述设定、整理角色、修订正文

## 执行入口流程

### 第一步：检查项目文件夹

1. 检查当前目录是否存在项目文件夹（如 `作品名/` 或 `novel/`）
2. 检查是否存在 `README.md` 作为项目标识

### 第二步：分支处理

**新项目（不存在）：**
1. 询问作品名称
2. 按目录结构初始化项目
3. 引导作者提供作品定位
4. 生成四类设定文件模板
5. 询问是否创建 `.rules/` 规则文件（可从默认模板生成）
6. Git 初始化并首次 commit

**已有项目（存在）：**
1. 读取 `.rules/` 下所有规则文件（包括自定义规则）
2. 读取 `作品名-作品定位.md`
3. 读取 `作品名-剧情大纲.md`
4. 确认后执行用户命令

### 第三步：执行用户命令

按用户意图执行素材归档、正文创作、修订、续写等任务。

## 命令参考

自然语言触发为主，以下命令用于需要精确控制时：

| 命令 | 等价自然语言 | 做什么 |
|------|-------------|--------|
| `/nova init` | 开个新小说 / 初始化项目 | 创建目录结构 + 四类设定模板 + `.rules/` + git init |
| `/nova feed` | 帮我整理这些设定 | 读取原始素材 → 提取信息 → 归类写入设定文件 |
| `/nova rev` | 把XX改成YY / 全局替换 | 全局搜索受影响位置 → 列出清单 → 批量修改 → commit |
| `/nova write` | 写第X章 / 继续写 | 读取大纲 + 规则 → 按文风规范生成正文 |
| `/nova cont` | 继续上次写 | 读取断点标记 → 回顾上下文 → 续写 |
| `/nova done` | 今天先到这 | 标注进度断点 → commit → push |
| `/nova stat` | 进度 / 写到哪了 | 显示当前章节位置 + 最近 commits + 变更摘要 |
| `/nova rule` | 加条规则 / 改禁止事项 | 引导创建或修改 `.rules/` 规则文件 → commit |

## 资源索引

- 详细工作流程：见 [references/workflow.md](references/workflow.md)
- 规则引擎详解（高度可扩展）：见 [references/rules.md](references/rules.md)
- 使用示例：见 [references/usage-examples.md](references/usage-examples.md)
- 默认模板：见 [references/templates/](references/templates/)
  - `规则引擎.md` - 规则引擎配置模板
  - `禁止事项.md` - 禁止事项模板
  - `输出格式.md` - 输出格式模板
  - `文风规范.md` - 文风规范模板
